apiVersion: v1
kind: Service
metadata:
  name: backend-feed
  namespace: default
  selfLink: /api/v1/namespaces/default/services/backend-feed
  labels:
    app: backend-feed
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 80
  selector:
    app: backend-feed